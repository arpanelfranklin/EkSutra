apiVersion: v1
kind: Service
metadata:
  name: system-b-service
  namespace: govt-ip
  labels:
    app: system-b
spec:
  type: ClusterIP
  selector:
    app: system-b
  ports:
    - name: http
      protocol: TCP
      port: 8082
      targetPort: 8082
