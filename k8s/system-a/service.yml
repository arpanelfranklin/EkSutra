apiVersion: v1
kind: Service
metadata:
  name: system-a-service
  namespace: govt-ip
  labels:
    app: system-a
spec:
  type: ClusterIP
  selector:
    app: system-a
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
